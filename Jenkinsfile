pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                echo 'hello jenkins'
                sh 'pwd'
                sh 'ls -l'
            }
        }
    }
}
